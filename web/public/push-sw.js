self.addEventListener('push',event=>{let data={};try{data=event.data?event.data.json():{}}catch{data={body:event.data?.text()||''}};event.waitUntil(self.registration.showNotification(data.title||'TROVENDI',{body:data.body||'Появился новый доступный слот приёмки.',icon:'/trovendi-mark.svg',badge:'/trovendi-mark.svg',tag:data.tag||'fbo-slot',renotify:true,data:{url:data.url||'/fbo-slots'}}))});self.addEventListener('notificationclick',event=>{event.notification.close();const url=event.notification.data?.url||'/fbo-slots';event.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(list=>{for(const client of list){if('focus'in client){client.navigate(url);return client.focus()}}return clients.openWindow(url)}))});
